spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Feelow?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  profiles:
    include: real-db

  session:
    store-type: jdbc


